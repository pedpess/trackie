import React from "react";
import { View, StyleSheet, Text } from "react-native";

const TrackCreateScreen = () => {
  return <Text>TrackCreate Screen</Text>;
};

const styles = StyleSheet.create({});

export default TrackCreateScreen;
