import React from "react";
import { View, StyleSheet, Text } from "react-native";

const TrackDetailScreen = () => {
  return <Text>TrackDetail Screen</Text>;
};

const styles = StyleSheet.create({});

export default TrackDetailScreen;
