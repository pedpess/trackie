import React from "react";
import { View, StyleSheet, Text } from "react-native";

const TrackListScreen = () => {
  return <Text>TrackList Screen</Text>;
};

const styles = StyleSheet.create({});

export default TrackListScreen;
